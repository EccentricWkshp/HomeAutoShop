# Caddy's own CA signs the certificate. Nothing outside this machine trusts it
# until its root is installed there by hand, which is why this is the fallback
# rather than the recommendation: it is one install per device, forever, and on
# iOS it is buried behind an obscure second "trust" toggle most people never
# find. Use it when no public domain is available.
tls internal
